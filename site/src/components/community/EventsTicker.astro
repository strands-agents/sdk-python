---
import type { LearnEvent } from '../../content.config'
import { formatEventDate, toIsoDate } from '../../util/learn'
import { pathWithBase } from '../../util/links'

interface Props {
  events: LearnEvent[]
}

const { events } = Astro.props
---

{
  events.length > 0 && (
    <div class="ticker">
      <div class="inner">
        <b class="lbl">UPCOMING</b>
        {events.map((event) => {
          const expires = toIsoDate(event.endDate ?? event.startDate)
          const label = (
            <>
              <span class="date">{formatEventDate(event)}</span> {event.title}
            </>
          )
          return event.href ? (
            <a href={event.href.startsWith('http') ? event.href : pathWithBase(event.href)} data-expires={expires}>{label}</a>
          ) : (
            <span data-expires={expires}>{label}</span>
          )
        })}
        <a class="all" href="#events">
          All events →
        </a>
      </div>
    </div>
  )
}

<style>
  /* Renders inside the hero composition — no band chrome of its own. */
  .ticker {
    margin-top: 2.5rem;
    font-family: 'Figtree', sans-serif;
    font-size: 0.9rem;
    color: #a9c4b3;
  }

  :global([data-theme='light']) .ticker {
    color: var(--sl-color-gray-2);
  }

  .inner {
    display: flex;
    gap: 1.75rem;
    align-items: baseline;
    white-space: nowrap;
    overflow-x: auto;
    scrollbar-width: none;
  }

  .inner::-webkit-scrollbar {
    display: none;
  }

  .lbl {
    color: #00cc5f;
    letter-spacing: 0.15em;
    font-size: 0.7rem;
    flex-shrink: 0;
  }

  :global([data-theme='light']) .lbl {
    color: #008542;
  }

  .date {
    color: #d8b25e;
    font-weight: 700;
  }

  :global([data-theme='light']) .date {
    color: #8a6d1f;
  }

  a {
    color: inherit;
    text-decoration: none;
  }

  a:hover {
    color: #00ff77;
  }

  :global([data-theme='light']) a:hover {
    color: #008542;
  }

  .all {
    color: #00cc5f;
    font-weight: 600;
    flex-shrink: 0;
  }

  :global([data-theme='light']) .all {
    color: #008542;
  }

  @media (min-width: 901px) {
    .inner > a:not(.all),
    .inner > span {
      flex: 0 1 auto;
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
    }
  }
</style>
